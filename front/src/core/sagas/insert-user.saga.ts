import { actionIds } from "common-app";
import { takeEvery } from "redux-saga/effects";

/*
export function* watchInsertNewUserRequest() {
    yield takeEvery(
      actionIds.INSERT_NEW_USER,
      requestInsertNewUsers
    );
  }

  function* requestInsertNewUsers() {
    
     yield call()
  }*/
